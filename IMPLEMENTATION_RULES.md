# CodexTex 구현 규칙

이 문서는 CodexTex의 기능을 추가하거나 수정할 때 지켜야 하는 제품 및 구현 규칙이다.
에이전트 실행 지침이 아니며 `AGENTS.md`를 대신하지 않는다. 현재 코드와 이 문서가
충돌하면 의도하지 않은 동작 변경으로 간주하고, 먼저 차이를 확인한 뒤 코드 또는
문서를 함께 갱신한다.

## 1. 제품 경계

- CodexTex는 Windows 전용 독립형 네이티브 앱이다.
- 기본 기술 스택은 C++20, CMake/vcpkg, Win32, Direct3D 11, HLSL, Dear ImGui다.
- v1 편집 대상은 UV가 있는 OBJ 하나와 Base Color PNG 하나다.
- OBJ, UV, 카메라, 숨긴 면, 마스크, 탭 상태를 저장하는 프로젝트 포맷을 만들지 않는다.
- 사용자가 명시적으로 저장할 수 있는 영구 결과물은 편집된 PNG 텍스처뿐이다.
- OBJ와 MTL은 생성하거나 수정하지 않는다. MTL은 입력 연결에도 사용하지 않는다.
- 다중 재질, 다중 UV 세트, UV 편집, PBR 보조 맵은 별도 범위 승인 없이 추가하지 않는다.

## 2. 에셋과 상태 소유권

다음 용어를 일관되게 사용한다.

- `원본 텍스처`: 현재 세션에서 PNG를 처음 불러온 시점의 변경되지 않은 이미지다.
- `작업 텍스처`: 모든 베이크와 Undo/Redo가 적용되는 유일한 편집 대상이다.
- `프로젝션 이미지`: 한 프로젝션 탭에서 마스크를 거쳐 작업 텍스처에 베이크할 이미지다.
- `참고 에셋`: ImageGen의 주변 문맥을 제공하는 읽기 전용 OBJ와 PNG 한 쌍이다.

구현 시 다음 소유권을 지킨다.

- 모든 뷰포트와 프로젝션 탭은 하나의 작업 텍스처를 공유한다.
- 프로젝션 탭을 만들 때 작업 텍스처를 복제하거나 탭별 편집본을 만들지 않는다.
- 한 탭의 베이크 결과는 다른 모든 탭과 메인 뷰포트에 즉시 반영한다.
- Undo/Redo 기록도 탭별이 아니라 작업 텍스처 기준으로 공유한다.
- 원본 텍스처는 비교 표시용이며 베이크, Undo/Redo 또는 저장으로 덮어쓰지 않는다.
- 새 PNG를 불러올 때만 원본과 작업 텍스처를 함께 교체하고 텍스처 편집 기록을 초기화한다.
- OBJ와 PNG의 연결은 프로세스 메모리에서만 유지한다.

## 3. OBJ와 텍스처 입력

- OBJ와 PNG는 서로 독립된 버튼으로 불러온다.
- OBJ와 PNG가 모두 정상적으로 불러와진 적이 있으면 두 절대 경로를 하나의 최근 입력 쌍으로
  실행 파일 옆 설정 파일에 즉시 저장한다. 한쪽 경로만 저장하지 않는다.
- 저장된 두 파일이 모두 존재할 때만 `마지막 파일 불러오기` 버튼을 기본 입력 버튼 옆에 표시하며,
  이 버튼은 OBJ와 PNG를 항상 함께 불러온다.
- OBJ는 로드 시 삼각형화하며 UV가 없거나 인덱스가 잘못됐거나 UV가 v1 범위를 벗어나면
  로드를 막고 이유를 표시한다.
- 노멀이 없으면 계산한다.
- 겹치거나 미러링된 UV는 로드를 막지 않고 개수와 결과 위험을 경고한다.
- PNG의 해상도, RGBA 채널, sRGB 해석과 기존 알파를 유지한다.
- 미저장 작업 텍스처가 있으면 새 PNG 로드와 종료 전에 확인한다.
- 저장과 다른 이름으로 저장은 작업 텍스처만 PNG로 기록한다.
- 자동 저장, 사이드카 파일, 최근 프로젝트 파일은 만들지 않는다.

## 4. 뷰포트 표시

- 셰이딩은 기본적으로 끈다. 기본 화면은 텍스처의 Base Color를 조명 곱 없이 표시한다.
- 중립 셰이딩은 사용자가 필요할 때 켜는 표시 옵션이다.
- 배경은 검정에 가까운 색 대신 메시 밖임을 구분할 수 있는 단색을 기본으로 사용한다.
- 사용자는 `Viewport display`에서 배경 단색을 변경할 수 있으며 모든 뷰포트와 이후 캡처에
  같은 색을 적용한다. 배경색 변경은 메시 및 참고 에셋 텍스처에 영향을 주지 않는다.
- 모든 뷰포트는 기본적으로 현재 작업 텍스처를 렌더링한다.
- 메인 뷰포트는 `원본 텍스처 렌더링` 체크박스로 작업 텍스처와 로드 당시 원본을
  비교할 수 있다.
- 원본 표시 체크박스는 우측 도구 패널에 두지 않고 메인 뷰포트 우측 상단의 둥근
  반투명 오버레이 패널에 둔다.
- 원본 표시 옵션은 렌더링 소스만 바꾸며 작업 텍스처와 프로젝션 탭 상태를 변경하지 않는다.
- 프로젝션 탭은 탭별로 `작업`, `원본`, `생성 이미지`의 세 표시 상태를 보존한다.
  - `작업`은 기본 상태이며 공유 작업 텍스처 위에 현재 마스크 기반 투영 미리보기를 합성한다.
  - `원본`은 로드 당시 원본 텍스처만 표시하고 투영 미리보기를 끈다.
  - `생성 이미지`는 마스크를 무시하고 정사각형 크롭 안의 투영 가능 메시 표면을 생성 이미지로
    완전히 덮어 표시한다. 생성 이미지를 참고 에셋에는 적용하지 않는다.
- 프로젝션 탭의 세 상태 전환 라디오 버튼은 뷰포트 우측 상단의 둥근 반투명 오버레이
  패널에 둔다. 생성 이미지가 준비되기 전에는 `생성 이미지`를 비활성화한다.

- 표시 상태 오버레이 위의 포인터 입력은 브러시나 올가미로 전달하지 않는다.
- OBJ 로드 후 카메라는 경계 상자를 기준으로 자동 피팅하며 `F`로 다시 피팅할 수 있어야 한다.
- 카메라는 orbit, pan, zoom을 지원한다.
- 뷰포트 리사이즈나 탭 전환 시 현재 탭의 카메라, 숨김, 마스크, 로컬 면 필터,
  참고 에셋 표시 여부를 렌더러에 다시 적용한다.

## UI 언어

- 영어, 일본어, 한국어를 지원하며 첫 실행은 Windows 사용자 로캘을 따른다.
- 감지된 언어 또는 저장된 언어 코드가 지원되지 않으면 영어로 폴백한다.
- 사용자가 `설정 > 언어`에서 선택한 값은 실행 파일 옆 `CodexTex.settings.json`에 즉시 저장하며,
  이후 실행부터 저장값을 우선한다.
- 언어 메뉴 항목은 팝업 없이 하위 메뉴에 `English`, `日本語`, `한국어`로 표시하고 현재 UI 언어와
  관계없이 각 언어의 고유 표기를 유지한다.
- 언어 저장은 생성 직전까지 보류 중인 Codex 모델·추론 강도 변경을 함께 저장하지 않는다.

## ImageGen 프롬프트 히스토리

- 실제 ImageGen 생성 요청 직전에 사용자 프롬프트를 실행 파일 옆 설정 파일에 최신순으로 저장한다.
- 같은 프롬프트를 다시 사용하면 중복 항목을 만들지 않고 맨 앞으로 이동하며 최대 50개를 보존한다.
- 히스토리 팝업은 각 프롬프트의 앞부분만 목록에 표시하고, 항목을 누르면 전체 문구를 입력란에 복원한다.
- 항목 선택만으로 팝업을 닫지 않는다. 선택 항목 오른쪽에만 삭제 버튼을 표시하며 팝업 외부를
  클릭했을 때 닫는다.
- 히스토리 삭제 저장은 생성 직전까지 보류 중인 Codex 모델·추론 강도 변경을 함께 저장하지 않는다.

## UI 안내문

- 섹션 전체를 설명하는 보조 문구를 패널 본문에 상시 노출하지 않는다.
- 해당 섹션 헤더 옆의 `i` 아이콘에 접고, 포인터를 올렸을 때 다국어 툴팁으로 표시한다.
- 상태, 경고, 오류처럼 사용자가 즉시 알아야 하는 메시지는 툴팁으로 숨기지 않는다.

## 5. DPI와 문자 입력

- 앱은 창 생성 전 per-monitor DPI awareness를 설정한다.
- 폰트와 ImGui 간격은 모니터 DPI에서 직접 다시 빌드하고 Windows 비트맵 확대에 의존하지 않는다.
- `WM_DPICHANGED` 후 폰트 아틀라스와 UI 크기를 새 DPI로 갱신한다.
- 한국어 IME 조합 완료 문자열을 UTF-8로 보존한다.
- 한글 완성형, 자모와 UI에 필요한 기호가 포함된 Windows 한글 폰트를 우선 사용한다.
- 고정 픽셀 UI를 추가할 때는 DPI 배율을 고려하되, 이미지 좌표와 텍스처 좌표는 명시적으로
  변환하여 UI 배율과 섞지 않는다.

## 6. 참고 에셋

- 참고 에셋은 OBJ와 PNG를 한 쌍으로 추가하며 여러 쌍을 허용한다.
- 참고 에셋은 작성된 월드 좌표를 유지하고 메인 메시의 스케일이나 중심에 자동 정렬하지 않는다.
- 참고 에셋은 뷰포트와 ImageGen 캡처에만 나타난다.
- 참고 에셋의 면 선택, 숨김, 프로젝션, 베이크, 저장을 허용하지 않는다.
- 프로젝션 페인팅 단계에서 참고 에셋을 강제로 숨기지 않는다. 사용자가 탭별로 표시 여부를
  선택할 수 있어야 한다.
- 프로젝션 탭 생성 당시 표시 상태를 보존하되 탭 안에서 사용자가 다시 변경할 수 있어야 한다.

## 7. 면 선택과 숨김

- 카메라 탐색 모드와 페이스 선택 모드를 분리한다.
- 페이스 선택은 단일 클릭과 화면 공간 올가미를 지원한다.
- `선택 숨김`, `직전 숨김 취소`, `모두 표시`를 제공한다.
- 숨긴 면은 컬러, 깊이, ID 렌더, 캡처와 베이크에서 모두 제외한다.
- 숨긴 면 뒤의 삼각형은 깊이 및 ID 버퍼에서 실제로 선택 가능하게 노출되어야 한다.
- 숨김은 런타임 상태이며 OBJ를 수정하거나 별도 파일에 저장하지 않는다.

## 8. 캡처와 프로젝션 탭

- 사용자가 별도의 캡처 단계를 수행하게 하지 않는다.
- 메인 뷰포트에서 생성을 누르는 순간 현재 뷰를 캡처하고 새 프로젝션 탭을 만든다.
- 외부 PNG 경로도 동일한 방식으로 현재 뷰를 동결한 프로젝션 탭을 만든다.
- ImageGen 입력과 출력은 항상 1:1 정사각형이다.
- 비정사각형 뷰포트는 중앙 정사각형을 크롭하며 정확한 범위를 뷰포트 프레임으로 표시한다.
- 프로젝션 탭은 카메라 행렬, 뷰포트 크기, 크롭, 깊이, 삼각형 ID, 숨김 상태와 참고 에셋
  표시 상태를 생성 시점에 동결한다.
- 동결된 프로젝션 탭에서는 카메라와 기본 메시 가시성을 변경하지 않는다.
- 생성 중인 탭만 잠그며 메인 뷰포트와 다른 탭은 계속 사용할 수 있어야 한다.
- 여러 생성 작업을 동시에 시작할 수 있으며 각 탭은 독립된 Codex job, thread, turn과
  진행 상태를 가져야 한다.
- 탭을 닫거나 임시 입력 파일을 삭제하면 연결된 AI 작업을 취소하고 런타임 상태를 정리한다.
- 생성 이미지가 정사각형이 아니면 투영을 막고 재생성 또는 올바른 외부 PNG를 안내한다.

## 9. 마스크 편집

- 마스크 편집 모드는 페이스 선택 모드와 분리한다.
- 왼쪽 버튼은 포함, 오른쪽 버튼은 지우기로 동작하는 화면 공간 브러시를 제공한다.
- 브러시 모드에서는 실제 적용 반경과 같은 Dab 원을 페인팅 뷰포트에 표시한다.
- 포함 Dab은 청록색, 지우기 Dab은 빨간색으로 구분하고 중심 표시를 제공한다.
- Dab과 실제 칠하기는 정사각형 프로젝션 크롭 밖으로 확장하지 않는다.
- 올가미는 포함과 제외를 모두 지원한다.
- 이진 마스크를 원본으로 유지하고 페더는 표시 및 베이크용 파생 결과로 계산한다.
- 페더는 안쪽 방향으로만 적용하며 선택 밖으로 마스크를 확장하지 않는다.
- 페더 범위는 0~128px, 기본값은 16px다.
- 페더 이후에도 깊이, 실루엣, 숨김, 앞면 및 표면 각도 유효성 마스크를 다시 적용한다.
- 마스크 범위는 사용자가 브러시 또는 올가미로 직접 결정한다.

## 10. UV 베이크

- 베이크는 동결 프레임의 clip 좌표를 사용해 메시를 UV 공간에 다시 래스터라이즈한다.
- 동결된 깊이와 일치하는 당시의 보이는 면만 허용한다.
- 뒷면을 제외한다.
- 과도하게 비스듬한 면은 기본 75도 임계값으로 제외하며 탭에서 조정 가능하게 한다.
- 마스크 알파로 기존 작업 텍스처와 합성한다.
- 선택 밖 픽셀과 기존 텍스처 알파를 보존한다.
- 겹친 UV는 자동 분리하지 않는다.
- 미러 UV 대응을 위해 로컬 X의 음수 또는 양수 한쪽을 베이크에서 제외하는 옵션을 제공한다.
- 베이크 전 오버레이 미리보기와 베이크 후 공유 Undo/Redo를 유지한다.

## 11. Codex와 ImageGen 연동

- OpenAI API를 직접 호출하거나 API 키를 입력, 저장 또는 대체 인증으로 사용하지 않는다.
- 로컬 Codex App Server가 기존 ChatGPT 구독 로그인을 재사용한다.
- 앱이 로그인 흐름을 시작하지 않는다. 미로그인이나 인증 만료 시 AI 기능만 막고 이유를 표시한다.
- `codex.exe`, `codex.cmd`, Codex 데스크톱 설치 경로를 순서대로 탐색할 수 있다.
- App Server 초기화 후 계정, `imagegen` 스킬과 필요한 프로토콜 기능을 점검한다.
- AI를 사용할 수 없어도 외부 PNG 프로젝션과 수동 마스크 기능은 계속 제공한다.
- 생성 입력에는 동결된 정사각형 캡처를 `localImage`로 전달하고 `imagegen` 스킬을 명시한다.
- 프롬프트는 카메라, 실루엣, 비율, 가림 경계와 픽셀 정렬을 유지하도록 요구한다.
- 배경 및 참고 에셋은 문맥으로만 사용하고 변경하지 않도록 요구한다.
- Base Color 용도이므로 강한 조명, 반사, 그림자와 AO가 이미지에 구워지지 않도록 요구한다.
- 결과는 `imageGeneration.savedPath` 이벤트에서 받고 세션 임시 디렉터리로 복사한다.
- 생성 취소는 해당 탭의 `turn/interrupt`로 처리한다.
- UI 스레드에서 App Server 파이프 읽기나 생성 완료를 동기 대기하지 않는다.

## 12. 모델과 리즈닝 설정

- 선택 가능한 모델과 effort는 가능한 경우 App Server의 `model/list` 결과로 구성한다.
- 설정 파일이 한 번도 기록되지 않았다면 기본값은 `gpt-5.6-sol`과 `medium`이다.
- 사용자가 선택을 바꿀 때는 우선 메모리 상태만 변경한다.
- 생성 버튼을 누르면 App Server 요청 직전에 현재 값을 실행 파일 옆
  `CodexTex.settings.json`에 기록한다.
- 설정 파일 저장에 실패하면 생성 요청을 보내지 않고 오류를 표시한다.
- `thread/start`에 모델을 명시하고 `turn/start`에는 모델과 effort를 모두 명시한다.
- 전역 Codex 설정이나 이전 세션의 effort가 암묵적으로 상속되게 두지 않는다.
- 프로젝션 탭은 생성 당시 사용한 모델과 effort를 기록하고 정보 패널에 표시한다.
- 모델 설정 파일은 사용자 로컬 상태이며 Git에 포함하지 않는다.

## 13. 로그와 임시 파일

- ImageGen 및 App Server 진단 로그는 실행 파일 옆 `CodexTex-ImageGen.log`에 기록한다.
- 로그에는 요청과 응답, 선택한 실행 명령, 결과 경로, 오류와 시각을 남기되 인증 비밀을
  새로 수집하거나 기록하지 않는다.
- 캡처, 생성 이미지와 런타임 마스크는 프로세스별 세션 임시 디렉터리에 둔다.
- `Session Temp` UI에서 파일 목록, 크기와 제한된 미리보기를 제공한다.
- PNG는 이미지로 미리보고 텍스트는 크기를 제한하여 표시한다.
- 개별 삭제와 전체 삭제를 지원한다.
- 사용 중인 파일을 삭제할 때는 연결된 탭과 AI 작업을 먼저 안전하게 정리한다.
- 전체 삭제는 명시적 확인 뒤에 수행하며 세션 디렉터리 자체는 유지한다.
- 정상 종료 시 세션 임시 디렉터리를 정리한다.

## 14. UI 배치 원칙

- 메인 OBJ/PNG 로딩 버튼은 메인 뷰포트 컨텍스트에서만 제공한다.
- 프로젝션 탭에서는 고정 OBJ, 텍스처, 삼각형 수, 캡처 크기, 숨긴 면, 모델과 effort를
  읽기 전용 정보로 표시한다.
- 생성 중에는 생성 버튼 위치에 `m:ss` 경과 시간을 표시하고 취소 버튼을 제공한다.
- 상태를 잠그는 경우 잠긴 범위와 이유를 같은 화면에 표시한다.
- 자주 보는 뷰포트 전용 옵션은 가능한 한 뷰포트 오버레이에 두고 전역 도구 패널을
  불필요하게 늘리지 않는다.
- 오버레이는 메시나 크롭의 중요한 부분을 과도하게 가리지 않는 코너에 배치하고,
  반투명 둥근 패널과 충분한 대비를 사용한다.
- 오버레이 위의 클릭과 드래그가 카메라 탐색, 면 선택 또는 마스크 칠하기로 전달되지 않게 한다.

## 15. 렌더러와 성능

- 렌더, ID, 깊이, UV 베이크와 마스크 페더는 가능한 한 D3D11 GPU 경로를 유지한다.
- UI 프레임마다 GPU 리소스를 불필요하게 재생성하거나 전체 텍스처를 CPU로 읽지 않는다.
- 탭별 동결 리소스와 전역 작업 텍스처의 수명을 명확히 분리한다.
- 렌더러의 전역 플래그는 탭 전환 시 누출될 수 있으므로 각 뷰포트 렌더 직전에 필요한
  상태를 명시적으로 설정한다.
- 목표 성능은 기준 GPU에서 100만 삼각형, 4K PNG, 1080p 뷰포트 조작 60fps,
  페더 미리보기 33ms 이내, 단일 베이크 1초 이내다.
- 성능 결과를 제시할 때는 GPU와 드라이버 또는 WARP 사용 여부를 함께 기록한다.

## 16. 오류 처리와 호환성

- AI 기능 오류가 메시 편집, 외부 PNG, 수동 마스크와 저장 기능을 함께 막지 않게 한다.
- 프로토콜 버전 차이는 안전한 범위에서만 명시적 fallback을 제공하고 진단 로그에 남긴다.
- 실패를 성공처럼 표시하지 않는다. 파일 저장, GPU 업로드, App Server 요청 결과를 확인한 뒤
  상태를 변경한다.
- 사용자의 원본 OBJ나 PNG를 암묵적으로 덮어쓰지 않는다.
- 파괴적 동작의 대상은 정확한 세션 경로인지 검증하고 가능하면 복구 가능한 흐름을 사용한다.

## 17. 코드 변경 규칙

- 상태의 소유자를 먼저 정한 뒤 UI, 애플리케이션, 렌더러 사이의 책임을 나눈다.
- UI 코드는 사용자 의도를 수집하고 상태를 표시하며, GPU 리소스 소유권은 렌더러에 둔다.
- 같은 의미의 상태를 탭과 렌더러에 중복 저장해야 할 때는 탭 상태를 원본으로 삼고 활성화 시
  렌더러에 복원한다.
- 파일 경로는 `std::filesystem::path`로 처리하고 Win32 경계에서는 UTF-16, JSON 경계에서는
  UTF-8 변환을 명시한다.
- 백그라운드 App Server 이벤트에는 job ID를 유지해 다른 탭으로 결과가 전달되지 않게 한다.
- 새 기능으로 기존 제품 규칙이 바뀌면 코드와 이 문서를 같은 변경 단위에서 갱신한다.

## 18. 검증 기준

- 코어 로직 변경에는 가능한 경우 단위 테스트를 추가한다.
- GPU 렌더 또는 베이크 변경은 D3D11 WARP 테스트로 재현 가능하게 검증한다.
- 최소 회귀 범위에는 OBJ 검증, PNG RGBA 왕복, 마스크, 프롬프트, App Server mock,
  동시 탭 라우팅, 숨김/깊이, 로컬 X 필터와 정사각형 크롭이 포함된다.
- 원본/작업 텍스처 선택처럼 렌더 결과가 달라지는 기능은 픽셀 결과로 검증한다.
- App Server mock은 실제 요청에 모델, effort, sandbox와 입력 이미지가 포함되는지 검증한다.
- 변경 완료 전 `git diff --check`, 테스트 전체 실행과 GUI 타깃 컴파일을 수행한다.
- 실행 중인 사용자의 앱이 빌드 결과를 잠근 경우 임의 종료하지 않는다. 별도 검증 출력으로
  링크하고 잠금 상태를 사용자에게 알린다.
